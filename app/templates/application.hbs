<nav>
    <div class="container">
        {{#link-to 'index'}}<h1>Weather App</h1>{{/link-to}}
        <ul class="right-nav">
            <li>
                {{input placeholder="search"}}
            </li>
            <li>
            {{current-location foundLocation="redirectWeather"}}
            </li>
        </ul>
    </div>
</nav>
{{outlet}}
